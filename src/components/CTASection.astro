---
interface Props {
  title: string;
  text: string;
  primaryLabel: string;
  primaryHref: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}

const {
  title,
  text,
  primaryLabel,
  primaryHref,
  secondaryLabel,
  secondaryHref,
} = Astro.props;
---

<section class="section cta-section">
  <div class="container">
    <div class="panel cta-panel">
      <div class="cta-copy">
        <p class="eyebrow cta-eyebrow">Free Residential Estimates</p>
        <h2>{title}</h2>
        <p>{text}</p>
      </div>
      <div class="btn-row cta-actions">
        <a class="btn cta-primary" href={primaryHref}>{primaryLabel}</a>
        {secondaryLabel && secondaryHref && <a class="btn cta-secondary" href={secondaryHref}>{secondaryLabel}</a>}
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding-top: clamp(3.1rem, 5vw, 4.8rem);
  }

  .cta-panel {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.4rem;
  }

  .cta-copy h2 {
    margin-bottom: 0.75rem;
    max-width: 16ch;
  }

  .cta-panel p {
    max-width: 60ch;
  }

  .cta-eyebrow {
    color: #c7def3;
    margin-bottom: 0.6rem;
  }

  .cta-actions {
    justify-content: flex-end;
    min-width: min(24rem, 100%);
  }

  .cta-primary {
    background: linear-gradient(180deg, #fff 0%, #f2f7fb 100%);
    color: #113a5f;
    border-color: #fff;
  }

  .cta-primary:hover {
    background: #f3f8fc;
    color: #0d2f4c;
  }

  .cta-secondary {
    background: rgb(255 255 255 / 8%);
    border-color: rgb(255 255 255 / 52%);
    color: #f4f9ff;
  }

  .cta-secondary:hover {
    background: rgb(255 255 255 / 16%);
    border-color: rgb(255 255 255 / 72%);
    color: #fff;
  }

  @media (max-width: 900px) {
    .cta-panel {
      flex-direction: column;
      align-items: flex-start;
    }

    .cta-copy h2 {
      max-width: none;
    }

    .cta-actions {
      width: 100%;
      min-width: 0;
      justify-content: flex-start;
    }
  }
</style>
